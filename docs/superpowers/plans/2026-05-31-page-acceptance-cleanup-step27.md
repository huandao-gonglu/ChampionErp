# Step 27 - 页面验收清理

## 目标

把发布页从“功能都有但调试味太重”收成更像实际 ERP 工作台的界面：主界面优先展示运营要用的卡片和动作，原始 JSON 与内部状态下沉到折叠区。

## 已完成

1. 发布页主视图改成业务优先：
   - 保留发布动作说明、平台预检卡片、发布前确认卡片、队列摘要、最近日志。
   - 不再把原始 `source / drafts`、队列 JSON、页面快照 JSON 直接摊在主面板。
2. 新增折叠式运维 / 调试面板：
   - `renderJsonDetailsCard()`
   - 商品结构快照折叠查看
   - 队列原始状态折叠查看
   - 发布页原始快照折叠查看
3. 发布页顶部说明改成运营语言：
   - 先预检
   - 再确认
   - 看结果
   - 明确标注 WB / Ozon 未接真实接口时不会写假成功
4. 队列状态展示从裸 JSON 改成摘要卡片：
   - 显示平台状态汇总、job_id、总状态
   - 原始状态仍可展开排查

## 验证

已通过：

- 前端内联脚本语法检查：`inline script syntax ok`
- Python 单测：`33 passed`
- Playwright 浏览器回归：`15 passed`
- `打开新版ERP.cmd` 未修改，时间仍为 `2026/5/27 13:53:20`

## 下一步建议

Step 28：做 Phase 1 总验收文档。

目标是把 Mercado Libre Mexico 闭环当前做到哪里、哪些是真实可用、哪些仍是边界或待接入，写成一份能直接接手的验收说明。

建议模型：5.4 高。这个步骤主要是收口总结、验收视角梳理和交接文档整理。
